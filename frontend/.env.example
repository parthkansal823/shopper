# Copy to frontend/.env

# Base URL of the backend API, no trailing slash.
# Local:      http://127.0.0.1:8000
# Production: https://shopper-backend-mqcf.onrender.com
#
# Vite inlines this at build time, so changing it requires a rebuild/redeploy.
VITE_API_URL=http://127.0.0.1:8000
